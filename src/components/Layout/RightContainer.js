import React from 'react'

import * as Styled from './__styles__/RightContainer.styles'

const RightContainer = ({ children }) => {
	return <Styled.Root>{children}</Styled.Root>
}

export default RightContainer
