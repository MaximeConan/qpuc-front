import React from 'react'

import * as Styled from './__styles__/LeftContainer.styles'

const LeftContainer = ({ children }) => {
	return <Styled.Root>{children}</Styled.Root>
}

export default LeftContainer
